apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flask-app

labels:
- pairs:
    app: flask-app
    managed-by: kustomize

resources:
- namespace.yaml
- deployment.yaml
- service.yaml

configMapGenerator:
- name: flask-config
  literals:
  - app_env=base